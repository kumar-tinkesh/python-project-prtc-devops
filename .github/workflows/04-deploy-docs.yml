name: 04 - Deploy Documentation (Docs Repo)

# This workflow runs IN the docs repository, not the main repo
# Copy this to your-docs-repo/.github/workflows/deploy.yml

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [closed]
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.11"

# Sets permissions for GitHub Pages deployment
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    name: Build Documentation
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout docs repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Install dependencies
        run: |
          pip install mkdocs-material
          pip install -r requirements-docs.txt 2>/dev/null || true
      
      - name: Build documentation
        run: |
          mkdocs build --strict
      
      - name: Upload build artifacts
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      
      - name: Trigger search reindex
        if: env.ALGOLIA_API_KEY != ''
        env:
          ALGOLIA_API_KEY: ${{ secrets.ALGOLIA_API_KEY }}
          ALGOLIA_APP_ID: ${{ secrets.ALGOLIA_APP_ID }}
        run: |
          echo "Triggering search reindex..."
          curl -X POST "https://${ALGOLIA_APP_ID}.algolia.net/1/indexes/*/batch" \
            -H "X-Algolia-Application-Id: ${ALGOLIA_APP_ID}" \
            -H "X-Algolia-API-Key: ${ALGOLIA_API_KEY}" \
            -d '{"requests":[]}' || true
      
      - name: Post deployment summary
        run: |
          echo "## 🚀 Documentation Deployed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**URL:** ${{ steps.deployment.outputs.page_url }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Site deployed successfully to GitHub Pages!" >> $GITHUB_STEP_SUMMARY
